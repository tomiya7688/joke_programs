# Exotic Random Generator Pack 実装予定

更新日: 2026-08-26
対象: I:\scripts\jokes
予定モジュール: exotic_randoms.py

## 目的

物理現象、ゲーム、言語、計算機科学などの小さな世界をシミュレーションし、
その観測結果を「クセのある乱数」として返す Python ライブラリを作る。
一様分布からの意図的な偏りは欠陥ではなく、各アルゴリズムの個性とする。

## 共通設計

- 内部乱数源は secrets.token_bytes または os.urandom を利用する。
- SHA-256 または SHAKE-256 で seed を展開し、simulation 用の乱数列を作る。
- seed 未指定はOSエントロピー、seed 指定は再現可能な決定モードとする。
- algorithm_name をseedに混ぜ、同じseedでもアルゴリズムごとに列を分離する。
- 内部乱数源が安全でも、シミュレーション後の出力は暗号用途を保証しない。
- 各クラスは random() または next() で1値を返し、型・範囲・単位を固定する。
- 実時間・CPU性能に依存する方式は、その環境依存性を仕様に明記する。

## 実装対象

| # | クラス | 抽出元と意図する偏り |
|---:|---|---|
| 1 | DoublePendulumRandom | 二重振り子の3D先端座標。時間相関のあるカオス |
| 2 | BingoWinnerTurnRandom | ビンゴの最初の勝者までのターン。中盤に山 |
| 3 | KettleEvaporationRandom | 熱対流・炎・気流による蒸発完了時刻 |
| 4 | ShiritoriSuddenDeathRandom | しりとりが「ん」で終わるまでのターン |
| 5 | AIFrogJump3DRandom | 標的を追うカエルの3D放物線ジャンプ時間 |
| 6 | VirtualMachineSortRandom | 仮想CPUのソート総命令数 |
| 7 | StdDevConstrainedRandom | 合計10000の100要素配列の標準偏差 |
| 8 | RPGLoserDamageBattleRandom | 4対4戦闘の敗者総ダメージを-1〜+1へ正規化 |
| 9 | MonteCarloPiDigitRandom | 一定時間のπ推定で到達した最大一致桁数 |
| 10 | MobGapSacrificeRandom | 穴越え成功までに落ちた2Dモブの数 |

## アルゴリズム方針

- DoublePendulumRandom: 角度・角速度・重力・減衰・外力を固定刻みで積分する。
  減衰や外力がない場合は「アトラクタ」と呼ばず、カオス的軌道と説明する。
- BingoWinnerTurnRandom: カード生成・抽選・勝者判定を1ゲームとして実行し、
  20〜40ターン付近に確率の山ができるよう調整する。
- KettleEvaporationRandom: 水量・液温・火力・室温・気流を1秒刻みで更新する。
- ShiritoriSuddenDeathRandom: 単語帳をデータ分離し、ループ・語彙不足・上限を処理する。
- AIFrogJump3DRandom: 標的へ近づくAIと、初速・重力・空中制御の物理を分離する。
- VirtualMachineSortRandom: 仮想命令セットでクイックソートし、比較・交換・分岐を数える。
  用途は手続き生成や計算量由来ノイズとし、暗号の種とは表現しない。
- StdDevConstrainedRandom: 非負の100要素を合計10000に正規化して標準偏差を返す。
- RPGLoserDamageBattleRandom: 攻撃・防御・スキル・会心を含む4対4を行い、敗者を採用する。
- MonteCarloPiDigitRandom: π推定の最大一致桁数を返し、best_digits・samples・best_piを任意公開する。
- MobGapSacrificeRandom: 速度・初速・重力・当たり判定を持つモブを順に投入し、落下数を返す。
  最大試行数を設け、成功窓は判定全体の10〜30%を目安にする。

## 共通APIと構成案

- 各クラスは seed、パラメータ、必要なら時間・試行上限を受け取る。
- random()/next() は値を返し、debug()/metadata() で生成過程を確認できるようにする。
- exotic_randoms.py: 公開クラス、共通結果型、公開例外
- wrapper.py: 名前付きアルゴリズムの登録・生成・一括呼び出し
- entropy.py: OSエントロピー、ハッシュ展開、アルゴリズム別seed分離
- simulations/: 各アルゴリズムの内部処理
- data/: 単語帳、モンスター定義などの編集可能データ
- tests/: 再現性、境界値、分布、性能テスト

## 実装フェーズ

1. 共通API、seedモード、アルゴリズム別乱数列を実装する。
2. wrapper.py の登録・生成・名前呼び出し・一括呼び出しを実装する。
3. 1〜8の基本アルゴリズムと決定的テストを実装する。
4. 9〜10を追加し、時間・試行上限と環境依存性を調整する。
5. 一様乱数とのヒストグラム比較、100000回規模の分布テストを追加する。
6. README、使用例、データの出典・ライセンスを整備する。

## 受け入れ基準

- 同じseed・設定で同じ結果を再現でき、異なるアルゴリズムの列は独立する。
- 各クラスの返却型、範囲、単位がテストで保証される。
- 無限ループせず、時間または試行回数の上限で終了する。
- 10種それぞれに抽出元・偏り・用途の説明がある。
- 内部乱数源の安全性と、偏った出力の性質を混同しない。

## 注意事項

出力を暗号鍵、トークン、パスワード、セキュリティ判定に使わない。
「乱数のために乱数で世界を作る」という冗長さは、本プロジェクトの個性として残す。
